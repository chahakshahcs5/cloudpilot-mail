{
  "name": "cloudpilot-mail",
  "version": "1.0.0",
  "description": "CloudPilot Mail — Your Cloudflare Temp Email Command Center",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@types/chrome": "^0.2.5",
    "esbuild": "^0.25.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
